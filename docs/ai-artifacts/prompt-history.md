# История промптов ИИ

1. Проверить текущую инфраструктуру и локальную конфигурацию. Исходная информация в README.md. Обновить ридми при необходимости
2. Реализовать консольную команду импорта: получать исходные данные из `https://gitlab.fdw.ru/ext/test_task` по пути из config/env, обрабатывать JSON и добавлять данные в БД без публикации изображений на этом этапе.
3. Добавить в skill миграций проверку применимости через `canRunMigration()` и применить это правило к текущим миграциям.
4. Добавить в README ссылки на настройки ИИ-агента и project-specific skills.
5. Обновить skill тестов: использовать `camelCase`, `TestDox` и private helpers для fixture-данных; применить правила к текущим тестам.
6. Для `cars` типизировать `custom_status` enum-ом.
7. Ввести value object `Money` на `BigDecimal` с валютой USD по умолчанию для денежных полей автомобилей.
8. Не прерывать импорт из-за ошибки одного исходного файла: пропускать проблемную запись и продолжать обработку остальных.
9. Реализовать потоковый импорт с generator, configurable чанками и batch `upsert` для неизвестных объёмов данных.
10. Сделать импорт устойчивым к ошибке DB batch: повторять batch и изолировать проблемную запись без остановки следующих чанков.
11. По ревью: отклонять дубли `AuctionItemId`, публиковать изображения только после сохранения записи, завершать полностью невалидный импорт с ошибкой.
12. Завершить основной backend: добавить сущность голосований и JSON-endpoint'ы для автомобилей, фильтрации статистики и голосования для последующего frontend-отображения.
13. Привести endpoint flow к DTO-контрактам: принимать и возвращать DTO, перенести валидацию входных запросов на уровень DTO.
14. Добавить защиту от накрутки голосов, единый JSON-формат ошибок и обновить текущую миграцию с локальным откатом при необходимости.
15. Кешировать список моделей автомобилей и инвалидировать кеш после импорта.
16. По независимому ревью: новый цикл голосования определять новой session, изолировать ошибки импорта, унифицировать JSON-ошибки.
17. Реализовать основу frontend-части голосования на jQuery и статистики на Vue.js.
18. На странице голосования заменить прямой перезапуск цикла подтверждающим popup-окном.
19. Сохранять выбранную модель голосования в localStorage и применять её как активный фильтр на странице статистики.
20. Для модели с одним автомобилем показывать одиночную карточку без возможности голосования.
21. Провести рефакторинг и оптимизацию JavaScript: выделить класс для голосования и вынести все его тексты в отдельную локаль.
22. Добавить клиентские API-адаптеры для Vue и jQuery: нормализовать JSON-ответы в UI-модели и изолировать transport-контракт.
23. По независимому ревью устранить риски импорта изображений и миграций, сделать цикл голосования масштабируемым и идемпотентным, добавить limiter пар и недостающие тесты.
